package comoViajo

object ws {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(74); 
  println("Welcome to the Scala worksheet");$skip(36); val res$0 = 
  
  
  List(1,2,3,4).minBy(_ * -1);System.out.println("""res0: Int = """ + $show(res$0))}
  
}
